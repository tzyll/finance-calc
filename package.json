{
  "name": "money-calculators",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "High-CPM finance calculator + content site. Static, zero-dependency, deploy anywhere.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && node serve.mjs",
    "dev": "node build.mjs && node serve.mjs",
    "test": "node --test"
  }
}
